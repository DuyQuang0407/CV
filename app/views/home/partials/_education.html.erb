<div class="education-section px-3 px-lg-4 pb-4">
    <h2 class="h3 mb-4">Education</h2>
    <div class="timeline">
      <% @educations.each do |education| %>
          <%= render partial: 'home/partials/education_item', locals: education.as_json%>
      <%end%>
    </div>
</div>